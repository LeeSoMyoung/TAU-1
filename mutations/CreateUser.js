import gql from 'graphql-tag';

export default gql `
    mutation createUser(
        $email:String!
        
    ){
        createRooms(
            input:{
                
            }
        ){
            
        }
    }
`;